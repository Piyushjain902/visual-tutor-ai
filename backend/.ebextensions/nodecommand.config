option_settings:
  aws:elasticbeanstalk:application:environment:
    NODE_ENV: production
    PORT: 8080
    AWS_REGION: ap-south-1
    # AWS credentials should be set via IAM role or EB environment configuration
    # Do not commit credentials to version control
